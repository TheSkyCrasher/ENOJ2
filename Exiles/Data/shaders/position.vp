#version 400

layout (location = 0) in vec3 position;

uniform mat4 MVP;
uniform mat4 MP;

out vec4 fPosition;

void main()
{ 
    gl_Position = MVP * vec4(position, 1.0);
    fPosition = MP * vec4(position, 1.0);
}